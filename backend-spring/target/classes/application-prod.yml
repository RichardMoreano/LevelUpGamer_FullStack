spring:
  # DataSource configuration handled by DatabaseConfig.java
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: ${PORT:8080}

logging:
  level:
    root: INFO
    '[cl.duoc.levelup]': INFO